# Medbot 开发与主干保护规范

## 分支原则

`main` 是受保护的可发布主干，只接收通过拉取请求验证后的改动。新功能、修复、重构和文档调整均不得直接在 `main` 开发。

开始工作前：

1. 拉取最新 `main`。
2. 从 `main` 创建独立主题分支。
3. 完成开发、测试和自检。
4. 推送主题分支并创建拉取请求。
5. CI 通过、讨论解决后再合并。

Codex 创建的分支统一使用 `codex/` 前缀，例如：

- `codex/ai-project-recruitment`
- `codex/fix-points-settlement`
- `codex/update-community-copy`

## 拉取请求要求

每个拉取请求应当：

- 只解决一个清晰目标。
- 说明业务背景、主要改动、数据迁移和验证方法。
- 对数据库变更提供 Prisma migration。
- 对界面变更提供本地及生产构建验证结果。
- 不包含 `.env`、数据库、密钥、内部原始文档或临时文件。
- 在合并前解决所有审查讨论。

## 必须通过的检查

```bash
npm run lint
npx tsc --noEmit
npx prisma validate
npm run build
```

GitHub Actions 中的 `quality` 检查会执行同等验证。检查失败时不得合并。

## 主干保护规则

GitHub 的 `main` 分支应启用：

- 必须通过拉取请求合并。
- 必须通过 `quality` 状态检查。
- 分支必须保持为最新后才能合并。
- 必须解决全部审查讨论。
- 禁止强制推送。
- 禁止删除主干。
- 规则对仓库管理员同样生效。

当前按单维护者场景设置为不强制他人批准，避免仓库所有者无法合并自己的拉取请求。团队增加代码审查人后，应将最低批准数提高到 1。

## 发布和数据库

- 发布前先备份生产 SQLite 数据库。
- 数据库结构变更必须通过 Prisma migration 管理。
- 容器重建后验证本地地址和公网地址。
- 内容型种子脚本应保持幂等，重复执行不得创建重复记录。
